# MindMate frontend — copy this file to `.env` and adjust as needed.
# `.env` is gitignored. NEVER commit your real .env to git.

# Base URL of the BACKEND API (Express server).
# Local dev: http://localhost:3000
# Production: https://api.your-domain.com  (or your Railway URL)
VITE_API_URL=http://localhost:3000
